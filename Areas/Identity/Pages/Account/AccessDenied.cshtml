@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access denied";
}

<main class="h-full w-full flex">
    <div class="m-auto text-red-500">
        <p class="text-2xl font-bold">@ViewData["Title"]</p>
        <p class="text-lg">You do not have access to this resource.</p>
    </div>
</main>